import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    
};

export const addToWishListSlice = createSlice({
  name: "wishlist",
  initialState,
  reducers: {
  
}
})

export const { } = addToWishListSlice.actions;
export const addToWishListReducer = addToWishListSlice.reducer;

